# MongoDB URI — used to auto-load products on startup for model warm-up
# Same connection string as backend
MONGODB_URI=mongodb+srv://nexusadmin:yourpassword@cluster0.abc123.mongodb.net/ecommerce?retryWrites=true&w=majority

# Port (default 8000)
PORT=8000

# Log level
LOG_LEVEL=info
